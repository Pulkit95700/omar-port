/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ["fixthephoto.com", "rvu.edu.in", "fakestoreapi.com"],
    },
};

module.exports = nextConfig;
